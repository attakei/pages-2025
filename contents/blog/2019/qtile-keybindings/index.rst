=====================================
Qtileでキーバインディングの設定をする
=====================================

.. post:: 2019-07-08
   :category: Tech
   :tags: ArchLinux,Python,Qtile,

最近個人PCのデスクトップマネージャーをQtileに切り替えたので、
キーバインディングの設定を ``xmonad`` のものから引っ越した。

その際にマルチメディアキーをきちんと拾ってくれるので、
こちらでも変わらずにキーボード経由でミュートのオン/オフなどを行っている。

その際の、 ``config.py`` がこちら（一部抜粋）

.. literalinclude:: ./config.py
   :language: python

修飾キーは不要なのだが、``Key`` 生成の際にはデフォルト引数が無いため、
「一切使わない」ことを示すように ``[]`` を指定している。

そこから先は、「どのマルチメディアキーを押したら」「どんな動作をするか」を順に定義。
自機には、 ``PulseAudio`` と ``light`` をインストールしており、コマンドを直接実行している。
